#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.precision(10);

    double a, b;
    cin >> a >> b;

    cout << (double) a / b << "\n";

    return 0;
}