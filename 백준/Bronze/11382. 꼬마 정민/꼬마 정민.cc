#include <iostream>

using namespace std;

long long a, b, c;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> a >> b >> c;
    cout << a + b + c << "\n";

    return 0;
}